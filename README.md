
  # TollPilot-UI

  This is a code bundle for TollPilot-UI. The original project is available at https://www.figma.com/design/jfHE8WiR8PG9cAhZ7uWaZ2/TollPilot-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  